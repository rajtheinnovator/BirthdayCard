<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/holo_blue_bright</item>
        <item name="colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="colorAccent">@android:color/holo_blue_light</item>
    </style>

    <style name="ParentLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="Picture">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@drawable/birthday</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="TopText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:textSize">@dimen/CardFontSize</item>
        <item name="android:layout_marginRight">@dimen/TopTextRPad</item>
        <item name="android:layout_marginLeft">@dimen/TopTextLPad</item>
        <item name="android:layout_marginTop">@dimen/TopTextTPad</item>
        <item name="android:textStyle">italic</item>
        <item name="android:layout_alignParentRight">true</item>
    </style>

    <style name="BottomText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:textSize">@dimen/CardFontSize</item>
        <item name="android:layout_marginLeft">@dimen/BTextLPad</item>
        <item name="android:layout_marginBottom">@dimen/BTextBPad</item>
        <item name="android:layout_marginRight">@dimen/BTextRPad</item>
        <item name="android:textStyle">italic</item>
    </style>

</resources>
